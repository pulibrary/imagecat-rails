<h1 id="list_header"> Guide Card Index </h1>

<% @guide_cards.each do |card| %>
  <h2 id="list_entries"> <%= link_to card.heading, guide_card_path(card.id) %> </h2>
<% end %>

<%= paginate @guide_cards %>